
version: 2

models:
  - name: prices
    description: "prices table"
    columns:
      - name: id
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null
